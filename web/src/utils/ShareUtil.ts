module tools {
    ///////////存放工具中共享变量的地方

    /**共享的点 */
    export let SharePoint:egret.Point = new egret.Point();
    /**共享的点2 */
    export let SharePoint2:egret.Point = new egret.Point();
    /**共享的矩形 */
    export let ShareRect:egret.Rectangle = new egret.Rectangle();
    /**共享的矩阵 */
    export let ShareMatrix:egret.Matrix = new egret.Matrix();
}