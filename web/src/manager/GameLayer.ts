module tools {
    /** 舞台层级 */
        export const enum GameLayer {
            /** 主界面 */
            mainLayer,
            /** 弹出层 */
            popLayer,
            /** tip层 */
            tipLayer,
        }
    }